import { ChangeColorIntervalDirective } from './change-color-interval.directive';
import { ElementRef } from '@angular/core';

xdescribe('ChangeColorIntervalDirective', () => {
  it('should create an instance', () => {
    // const directive = new ChangeColorIntervalDirective(new ElementRef());
    // expect(directive).toBeTruthy();
  });
});
